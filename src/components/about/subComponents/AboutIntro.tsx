import SmartLink from "@/components/common/SmartLink";
import { ArrowIcon, ButtonArrowIcon } from "@/svg";

const AboutIntro = () => {
    return (
        <div className="tp-about-title-wrap mb-30">
            <h2 className="tp-section-title tp-text-perspective">
                At Nexus, we don&apos;t just build websites or apps, we design
                <span style={{ color: "var(--tp-theme-primary)" }}> intelligent digital solutions that transform businesses.</span>
                            </h2>

        </div>
    );
};

export default AboutIntro;
